server:
  port: 8081

weather:
  api-key:
  url: https://api.openweathermap.org/data/2.5/weather

translator:
  en-url: http://localhost:8080/en/translate

spring:
  liquibase:
    default-schema: public
    enabled: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5430/weather_db
    username: weather
    password: weather
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

url:
